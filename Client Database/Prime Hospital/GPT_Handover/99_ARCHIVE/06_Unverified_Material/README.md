# Unverified Material

This folder contains claim and concept records that must not be treated as facts. It is not a dumping ground; each item should have an owner and verification action.

The original unchanged source files remain in `00_SOURCES/Original_Uploads`.
