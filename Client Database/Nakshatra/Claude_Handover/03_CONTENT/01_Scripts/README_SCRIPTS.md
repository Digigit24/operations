# SCRIPTS — status

⚠️ **No full scripts exist in the project.** The file named `Nakshatra_social_media_video_scripts.pdf` is **mis-named** — it actually contains the Content Performance Analysis report (page images + OCR), not scripts. See `../../07_REPORTS/01_Social_Media/`.

What DOES exist that is script-like:
- **Reel HOOKS / openings** of the 6 published reels — in `../04_Hooks_And_Headlines/Hooks_And_Headlines.md` (section A) and `../10_Published_Content/Published_Reels.md`.
- **Ad copy / banner layouts** for the 3 campaigns — in `../08_Ad_Copy/`. These are the closest thing to written "scripts" (headline + supporting line + body + CTA per concept).
- **Hook formulas & series ideas** — in `../04_Hooks_And_Headlines/` and `../03_Content_Ideas/`.

To rebuild real scripts: take a Content Idea (register) → apply a hook formula (section B/D) → structure with the four-act patient-story arc (Master Brand Brain §4.10) or the misdiagnosis-revelation arc (performance report Pattern 1) → end with a share-driving + comment-driving CTA. Name each new file: `YYYY-MM-DD_Topic_Content-Type_Language_Status_v01.md`.
